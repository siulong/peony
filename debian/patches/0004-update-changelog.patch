From: Yue-Lan <lanyue@ubuntukylin.com>
Date: Fri, 6 Jan 2023 11:44:39 +0800
Subject: update changelog.

---
 libpeony-qt/model/file-item-model.cpp   | 11 ++++++++++-
 peony-qt-desktop/desktop-item-model.cpp | 11 ++++++++++-
 src/control/tab-widget.cpp              |  3 ++-
 3 files changed, 22 insertions(+), 3 deletions(-)

diff --git a/libpeony-qt/model/file-item-model.cpp b/libpeony-qt/model/file-item-model.cpp
index 9f3990a..a09f22a 100644
--- a/libpeony-qt/model/file-item-model.cpp
+++ b/libpeony-qt/model/file-item-model.cpp
@@ -584,9 +584,18 @@ bool FileItemModel::dropMimeData(const QMimeData *data, Qt::DropAction action, i
     //NOTE:
     //do not allow drop on it self.
     auto urls = data->urls();
+    if (urls.isEmpty())
+        return false;
 
-    QStringList srcUris;
+    bool hasPeonyEncodedUris = false;
     if (data->hasFormat("peony-qt/encoded-uris")) {
+        if (!data->data("peony-qt/encoded-uris").isEmpty()) {
+            hasPeonyEncodedUris = true;
+        }
+    }
+
+    QStringList srcUris;
+    if (hasPeonyEncodedUris) {
         srcUris = QString(data->data("peony-qt/encoded-uris")).split(" ");
         for (QString uri : srcUris) {
             if (uri.startsWith("recent://"))
diff --git a/peony-qt-desktop/desktop-item-model.cpp b/peony-qt-desktop/desktop-item-model.cpp
index 5cf7c87..ed52c38 100644
--- a/peony-qt-desktop/desktop-item-model.cpp
+++ b/peony-qt-desktop/desktop-item-model.cpp
@@ -840,9 +840,18 @@ bool DesktopItemModel::dropMimeData(const QMimeData *data, Qt::DropAction action
     //NOTE:
     //do not allow drop on it self.
     auto urls = data->urls();
+    if (urls.isEmpty())
+        return false;
 
-    QStringList srcUris;
+    bool hasPeonyEncodedUris = false;
     if (data->hasFormat("peony-qt/encoded-uris")) {
+        if (!data->data("peony-qt/encoded-uris").isEmpty()) {
+            hasPeonyEncodedUris = true;
+        }
+    }
+
+    QStringList srcUris;
+    if (hasPeonyEncodedUris) {
         srcUris = QString(data->data("peony-qt/encoded-uris")).split(" ");
         for (QString uri : srcUris) {
             if (uri.startsWith("recent://"))
diff --git a/src/control/tab-widget.cpp b/src/control/tab-widget.cpp
index b2fc751..f3555b2 100644
--- a/src/control/tab-widget.cpp
+++ b/src/control/tab-widget.cpp
@@ -824,7 +824,8 @@ void TabWidget::slot_responseUnmounted(const QString &destUri, const QString &so
         {
             //all window accessed mount path should goto self top path，related to bug#104551
             if((Peony::GlobalSettings::getInstance()->getValue("LAST_FOCUS_PEONY_WINID") == dynamic_cast<MainWindow *>(this->topLevelWidget())->winId()
-                ||KWindowSystem::hasWId(dynamic_cast<MainWindow *>(this->topLevelWidget())->winId()))
+                ||KWindowSystem::hasWId(dynamic_cast<MainWindow *>(this->topLevelWidget())->winId())
+                ||QApplication::topLevelWidgets().contains(this->topLevelWidget()))
                     && index == currentIndex
                     && (decodedSrcUri == uri || Peony::FileUtils::isRemoteServerUri(decodedSrcUri))){/* 远程服务进入内部目录后卸载,link to bug#98623 */
                 qDebug()<<"sourceUri:"<<sourceUri<<"change to self top path"<<" index:"<<currentIndex;
