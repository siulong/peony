From: Yue-Lan <lanyue@ubuntukylin.com>
Date: Mon, 3 Jul 2023 12:01:12 +0800
Subject: update changelog.

---
 peony-qt-desktop/desktop-icon-view.cpp | 2 +-
 src/windows/x11-window-manager.cpp     | 1 +
 2 files changed, 2 insertions(+), 1 deletion(-)

diff --git a/peony-qt-desktop/desktop-icon-view.cpp b/peony-qt-desktop/desktop-icon-view.cpp
index f4ad594..30faa06 100644
--- a/peony-qt-desktop/desktop-icon-view.cpp
+++ b/peony-qt-desktop/desktop-icon-view.cpp
@@ -2018,6 +2018,7 @@ void DesktopIconView::dragMoveEvent(QDragMoveEvent *e)
         QHoverEvent he(QHoverEvent::HoverLeave, e->posF(), e->posF());
         viewportEvent(&he);
     }
+    e->setDropAction(action);
     if (e->isAccepted())
         return;
     qDebug()<<"drag move event" <<action;
@@ -2025,7 +2026,6 @@ void DesktopIconView::dragMoveEvent(QDragMoveEvent *e)
         e->accept();
         return QListView::dragMoveEvent(e);
     }
-    e->setDropAction(action);
     e->accept();
 }
 
diff --git a/src/windows/x11-window-manager.cpp b/src/windows/x11-window-manager.cpp
index 156027f..6ccd5c5 100644
--- a/src/windows/x11-window-manager.cpp
+++ b/src/windows/x11-window-manager.cpp
@@ -162,6 +162,7 @@ bool X11WindowManager::eventFilter(QObject *watched, QEvent *event)
                 auto widget = qobject_cast<QWidget *>(watched);
                 auto topLevel = widget->topLevelWidget();
                 topLevel->windowHandle()->startSystemMove();
+                m_is_draging = false;
 #else
                 //auto me = static_cast<QMouseEvent *>(event);
                 auto widget = qobject_cast<QWidget *>(watched);
