From: cckylin-cibot <cckylin-cibot@kylinos.cn>
Date: Wed, 13 Jul 2022 06:33:31 +0000
Subject: =?utf-8?b?Y2xvc2UtY2QgIzEyOTAzNyDjgJB3YXlsYW5k44CR44CQ5paH5Lu2566h?=
 =?utf-8?b?55CG5Zmo44CR5LuOVeebmOS4reaXoOazleaLluWKqOWkjeWItuaWh+S7tuWIsA==?=
 =?utf-8?b?57O757uf?=

---
 libpeony-qt/model/file-item-model.cpp   | 8 ++++++--
 peony-qt-desktop/desktop-item-model.cpp | 2 +-
 2 files changed, 7 insertions(+), 3 deletions(-)

diff --git a/libpeony-qt/model/file-item-model.cpp b/libpeony-qt/model/file-item-model.cpp
index 80c2842..694a43d 100644
--- a/libpeony-qt/model/file-item-model.cpp
+++ b/libpeony-qt/model/file-item-model.cpp
@@ -488,6 +488,7 @@ QMimeData *FileItemModel::mimeData(const QModelIndexList &indexes) const
     //set urls data URLs correspond to the MIME type text/uri-list.
     QList<QUrl> urls;
     QStringList uris;
+    QStringList encodedUris;
     for (auto index : indexes) {
         auto item = itemFromIndex(index);
         auto uri = item->m_info->uri();
@@ -497,11 +498,14 @@ QMimeData *FileItemModel::mimeData(const QModelIndexList &indexes) const
 
             urls << url;
             uris << uri;
+            auto encodeUri = Peony::FileUtils::urlEncode(uri);
+            encodedUris<<encodeUri;
         }
     }
     data->setUrls(urls);
     auto string = uris.join(" ");
-    data->setData("peony-qt/encoded-uris", string.toUtf8());
+    auto encodedString = encodedUris.join(" ");
+    data->setData("peony-qt/encoded-uris", encodedString.toUtf8());
     data->setText(string);
     return data;
 }
@@ -569,7 +573,7 @@ bool FileItemModel::dropMimeData(const QMimeData *data, Qt::DropAction action, i
 
     QStringList srcUris;
     if (data->hasFormat("peony-qt/encoded-uris")) {
-        srcUris = data->text().split(" ");
+        srcUris = QString(data->data("peony-qt/encoded-uris")).split(" ");
         for (QString uri : srcUris) {
             if (uri.startsWith("recent://"))
                 srcUris.removeOne(uri);
diff --git a/peony-qt-desktop/desktop-item-model.cpp b/peony-qt-desktop/desktop-item-model.cpp
index b52e6fb..cd01eef 100644
--- a/peony-qt-desktop/desktop-item-model.cpp
+++ b/peony-qt-desktop/desktop-item-model.cpp
@@ -813,7 +813,7 @@ bool DesktopItemModel::dropMimeData(const QMimeData *data, Qt::DropAction action
 
     QStringList srcUris;
     if (data->hasFormat("peony-qt/encoded-uris")) {
-        srcUris = data->text().split(" ");
+        srcUris = QString(data->data("peony-qt/encoded-uris")).split(" ");
         for (QString uri : srcUris) {
             if (uri.startsWith("recent://"))
                 srcUris.removeOne(uri);
